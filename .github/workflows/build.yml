name: CI Build

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build APK with Performance Flags
        run: ./gradlew :app:assembleRelease --parallel --build-cache --configure-on-demand

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Bannerlator-zh-APK
          path: app/build/outputs/apk/**/*.apk
